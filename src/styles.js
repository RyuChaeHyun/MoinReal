import { StyleSheet } from "react-native";

export const textStyles = StyleSheet.create({
    basicText: {
        padding: 10,
        fontSize: 25,
        fontWeight: '600',
        color: 'black',
    },
    errorText: {
        fontSize: 25,
        fontWeight: '700',
        color: 'red',
    }
})

